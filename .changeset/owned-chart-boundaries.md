---
"@moritzbrantner/charts": minor
---

Add explicit server-safe `core` and client-side `react` entrypoints, serializable chart view state, a semantic density-table fallback, and an internally owned Rust/WASM density acceleration seam without production-source `viz-engine` coupling.
